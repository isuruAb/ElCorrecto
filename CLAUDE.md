## Frontend

- If a piece of JSX is rendered more than once (even within the same file, e.g. a list item or panel heading), extract it into its own component under `frontend/src/components/` instead of defining it inline in the page file.
